<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.knue.commonfoundation.codes.CodeGroupManagementMapper">
    <select id="listCodeGroups" resultType="kr.ac.knue.commonfoundation.codes.CodeGroupRow">
        select cg.group_id as "groupId",
               cg.group_name as "groupName",
               cg.description as "description",
               cg.managing_department as "managingDepartment",
               cg.system_use_yn as "systemUseYn",
               cg.status as "status",
               count(dc.code_value) as "detailCodeCount",
               cg.created_at as "createdAt",
               cg.updated_at as "updatedAt"
        from code_groups cg
        left join detail_codes dc
          on dc.group_id = cg.group_id
         and dc.status != 'DELETED'
        <where>
            cg.status != 'DELETED'
            <if test="criteria.groupIdFilter != null and criteria.groupIdFilter != ''">
                and cg.group_id like concat('%', upper(#{criteria.groupIdFilter}), '%')
            </if>
            <if test="criteria.filter != null and criteria.filter != ''">
                and (cg.group_id like concat('%', upper(#{criteria.filter}), '%')
                    or cg.group_name like concat('%', #{criteria.filter}, '%')
                    or cg.description like concat('%', #{criteria.filter}, '%')
                    or cg.managing_department like concat('%', #{criteria.filter}, '%'))
            </if>
        </where>
        group by cg.group_id, cg.group_name, cg.description, cg.managing_department, cg.system_use_yn, cg.status, cg.created_at, cg.updated_at
        order by cg.group_id
        limit #{criteria.safeSize} offset #{criteria.offset}
    </select>

    <select id="findCodeGroupById" resultType="kr.ac.knue.commonfoundation.codes.CodeGroupRow">
        select cg.group_id as "groupId",
               cg.group_name as "groupName",
               cg.description as "description",
               cg.managing_department as "managingDepartment",
               cg.system_use_yn as "systemUseYn",
               cg.status as "status",
               count(dc.code_value) as "detailCodeCount",
               cg.created_at as "createdAt",
               cg.updated_at as "updatedAt"
        from code_groups cg
        left join detail_codes dc
          on dc.group_id = cg.group_id
         and dc.status != 'DELETED'
        where cg.group_id = #{groupId}
          and cg.status != 'DELETED'
        group by cg.group_id, cg.group_name, cg.description, cg.managing_department, cg.system_use_yn, cg.status, cg.created_at, cg.updated_at
    </select>

    <insert id="insertCodeGroup">
        insert into code_groups (group_id, group_name, description, managing_department, system_use_yn, status, created_by, updated_by, change_reason)
        values (#{groupId}, #{groupName}, #{description}, #{managingDepartment}, #{systemUseYn}, 'ACTIVE', #{createdBy}, #{createdBy}, #{changeReason})
    </insert>

    <update id="updateCodeGroup">
        update code_groups
        set group_name = #{groupName},
            description = #{description},
            managing_department = #{managingDepartment},
            system_use_yn = #{systemUseYn},
            status = case when #{systemUseYn} = 'Y' then 'ACTIVE' else 'INACTIVE' end,
            updated_at = CURRENT_TIMESTAMP,
            updated_by = #{updatedBy},
            change_reason = #{changeReason}
        where group_id = #{groupId}
          and status != 'DELETED'
    </update>
</mapper>
